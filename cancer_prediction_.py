# -*- coding: utf-8 -*-
"""cancer_prediction .ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1IaO5vIkadFrVr8l3MK1p__50BH7lPaF8
"""

# Commented out IPython magic to ensure Python compatibility.
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
# %matplotlib inline
import warnings
warnings.filterwarnings('ignore')

from sklearn.neighbors import KNeighborsClassifier
from sklearn.ensemble import RandomForestClassifier

data = pd.read_csv("Cancer_Data.csv")
print(data)

data.isnull().sum()

data.head()

data.tail()

data.info()

data.describe()



corr_matrix = data.corr()
print(corr_matrix)

data['diagnosis'] = data['diagnosis'].map({'M': 1, 'B': 0})
X = data[['fractal_dimension_mean']]
y = data['diagnosis']

#USING REGRESSION

from sklearn.linear_model import LogisticRegression

log_reg = LogisticRegression()

# Fit the model using the training data
log_reg.fit(X_train, y_train)

# Make predictions on the test set
y_pred_log = log_reg.predict(X_test)

# Calculate and print the accuracy
accuracy = metrics.accuracy_score(y_test, y_pred_log)
print(f"Logistic Regression Accuracy: {accuracy}")

# Example prediction with a single value (1 feature)
test_input = np.array([[0.06]]) # Providing 1 feature as expected by the model
prediction = log_reg.predict(test_input)

if prediction[0] == 1:
    print("Prediction: Person has high chance of cancer")
else:
    print("Prediction: Person has low chance of cancer")

#using knn

from sklearn.model_selection import cross_val_score
knn_scores = []
for k in range(1,21):
    knn_classifier = KNeighborsClassifier(n_neighbors = k)
    score=cross_val_score(knn_classifier,X,y,cv=10)
    knn_scores.append(score.mean())
plt.plot([k for k in range(1, 21)], knn_scores, color = 'red')
plt.xlabel('Number of Neighbors (K)')
plt.ylabel('Scores')
plt.title('K Neighbors Classifier scores for different K values')

knn = KNeighborsClassifier(n_neighbors = 5)
score=cross_val_score(knn_classifier,X,y,cv=10)
print(score.mean())

from sklearn.model_selection import train_test_split
X_train,X_test,y_train,y_test = train_test_split(X,y,test_size=0.2,random_state=1)
knn.fit(X_train,y_train)
y_pred = knn.predict(X_test)

from sklearn import metrics
print(metrics.accuracy_score(y_test,y_pred))

import numpy as np
test1 = (1)
test1 = np.asarray(test1)
test1 = test1.reshape(1,-1)
test1_predict = knn.predict(test1)
if test1_predict==1:
    print("Person has high chance of cancer")
else:
    print("Person has low chance of cancer")

from sklearn.neighbors import KNeighborsClassifier

# Initialize the KNeighborsClassifier again
knn = KNeighborsClassifier(n_neighbors = 5)

# Fit the model using the training data
knn.fit(X_train,y_train)

test2 = (0)
test2 = np.asarray(test2)
test2 = test2.reshape(1,-1)
test2_predict = knn.predict(test2)
if test2_predict==1:
    print("Person has high chance of cancer")
else:
    print("Person has low chance of cancer")

